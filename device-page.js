(() => {
  'use strict';
  const cfg=window.DDH_CONFIG||{};const base=String(cfg.supabaseUrl||'').replace(/\/$/,'');const key=String(cfg.supabasePublishableKey||'');const id=new URLSearchParams(location.search).get('id');const SESSION_KEY='ddh_supabase_session';const VISITOR_KEY='ddh_visitor_id';let listing=null;let seller=null;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=(a,c)=>{try{return new Intl.NumberFormat(undefined,{style:'currency',currency:c||'NGN',maximumFractionDigits:4}).format(Number(a)||0)}catch{return `${c||'NGN'} ${Number(a||0).toLocaleString()}`}};const publicImage=p=>p?`${base}/storage/v1/object/public/listing-images/${String(p).split('/').map(encodeURIComponent).join('/')}`:'';
  function session(){try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch{return null}}function visitor(){let v='';try{v=localStorage.getItem(VISITOR_KEY)||''}catch{}if(!/^[0-9a-f-]{36}$/i.test(v)){v=crypto.randomUUID();try{localStorage.setItem(VISITOR_KEY,v)}catch{}}return v}
  async function get(path,auth=false){const s=session();const headers={apikey:key};if(auth&&s?.access_token)headers.Authorization=`Bearer ${s.access_token}`;const r=await fetch(`${base}${path}`,{headers});if(!r.ok)throw new Error(`Request failed (${r.status})`);return r.json()}
  async function write(path,method,body){const s=session();if(!s?.access_token)throw new Error('Sign in to continue.');const r=await fetch(`${base}${path}`,{method,headers:{apikey:key,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify(body)});const data=await r.json().catch(()=>[]);if(!r.ok)throw new Error(data.message||data.error||`Request failed (${r.status})`);return data}
  function toast(t){const el=document.querySelector('#toast');el.textContent=t;el.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>el.classList.remove('show'),2600)}function modal(html){document.querySelector('#deviceDialogContent').innerHTML=html;document.querySelector('#deviceDialog').showModal()}
  async function recordRecentlyViewed(){const s=session();if(!listing||!s?.user?.id||!s?.access_token)return;try{await fetch(`${base}/rest/v1/recently_viewed`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json',Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({user_id:s.user.id,listing_id:listing.id,viewed_at:new Date().toISOString()}),keepalive:true})}catch{}}
  async function recordView(){if(!listing)return;const s=session();const country=String(window.DDH_LOCALIZATION?.state?.country||'').toUpperCase();fetch(`${base}/rest/v1/listing_events`,{method:'POST',headers:{apikey:key,'Content-Type':'application/json',Prefer:'return=minimal',...(s?.access_token?{Authorization:`Bearer ${s.access_token}`}:{})},body:JSON.stringify({listing_id:listing.id,visitor_id:visitor(),user_id:s?.user?.id||null,event_type:'view',country_code:/^[A-Z]{2}$/.test(country)?country:null}),keepalive:true}).catch(()=>{});recordRecentlyViewed()}
  function specRows(){const s=listing.specs||{};const rows=[['Category',listing.category],['Condition',listing.condition==='new'?'New':'Used'],['Brand',listing.brand],['Model',listing.model],['Storage',listing.storage],['Colour',listing.color],['Battery health',s.battery_health],['Network status',s.network_status],['Repairs',s.repair_history],['Included',s.accessories],['Delivery',String(listing.delivery_mode||'pickup').replaceAll('_',' ')],['Warranty',listing.warranty_text],['Location',[listing.city,listing.country_code].filter(Boolean).join(', ')]].filter(x=>x[1]);return rows.map(([a,b])=>`<div class="spec"><span>${esc(a)}</span><strong>${esc(String(b))}</strong></div>`).join('')}
  async function load(){if(!id||!/^[0-9a-f-]{36}$/i.test(id)){document.querySelector('#deviceMeta').innerHTML='<h1>Device not found.</h1>';return}try{await window.DDH_LOCALIZATION?.ready;const rows=await get(`/rest/v1/listings?select=id,seller_id,title,category,brand,model,condition,price_amount,price_currency,description,storage,color,city,country_code,delivery_mode,warranty_text,specs,created_at&status=eq.published&id=eq.${encodeURIComponent(id)}&limit=1`);listing=rows?.[0];if(!listing)throw new Error('This listing is no longer available.');const imgs=await get(`/rest/v1/listing_images?select=storage_path,sort_order&listing_id=eq.${encodeURIComponent(id)}&order=sort_order.asc`).catch(()=>[]);const profiles=await get(`/rest/v1/profiles?select=id,display_name,created_at&id=eq.${encodeURIComponent(listing.seller_id)}&limit=1`).catch(()=>[]);seller=profiles?.[0]||null;document.title=`${listing.title} — DigitalDeviceHub`;document.querySelector('#galleryMain').innerHTML=imgs?.[0]?.storage_path?`<img src="${esc(publicImage(imgs[0].storage_path))}" alt="${esc(listing.title)}">`:'<div style="font-size:72px;color:#8a919c">▯</div>';const sellerPrice=money(listing.price_amount,listing.price_currency);document.querySelector('#deviceMeta').innerHTML=`<span class="eyebrow">${esc(listing.category)} · ${esc(listing.condition==='new'?'New':'Used')}</span><h1>${esc(listing.title)}</h1><div class="subline">${esc(listing.storage||'Details available')}${listing.city?` · ${esc(listing.city)}`:''}</div><div class="device-price price" data-price-amount="${esc(listing.price_amount)}" data-price-currency="${esc(listing.price_currency)}">${esc(sellerPrice)}</div><div class="device-original">Seller's asking price: ${esc(sellerPrice)}</div>`;document.querySelector('#deviceDescription').textContent=listing.description||'The seller has not added a description yet.';document.querySelector('#deviceSpecs').innerHTML=specRows();document.querySelector('#sellerPanel').innerHTML=`<div class="spec"><span>Listed by</span><strong>${esc(seller?.display_name||'DigitalDeviceHub seller')}</strong></div><div class="spec"><span>Account status</span><strong>Email authenticated</strong></div><div class="spec"><span>Listing date</span><strong>${new Date(listing.created_at).toLocaleDateString()}</strong></div>`;window.DDH_LOCALIZATION?.refresh?.();recordView()}catch(err){document.querySelector('#deviceMeta').innerHTML=`<span class="eyebrow">Unavailable</span><h1>${esc(err.message)}</h1><p><a href="/marketplace.html">Return to marketplace →</a></p>`}}
  async function favorite(){const s=session();if(!s?.user?.id)return modal('<p class="eyebrow">Account required</p><h2>Sign in to save devices.</h2><p>Your saved devices appear in your account dashboard.</p><a class="btn" href="/?signin=1">Go to sign in</a>');try{await write('/rest/v1/favorites','POST',{user_id:s.user.id,listing_id:listing.id});toast('Saved to favorites.')}catch(err){if(/duplicate|unique/i.test(err.message))toast('Already in your favorites.');else toast(err.message)}}
  function message(){const s=session();if(!s?.user?.id)return modal('<p class="eyebrow">Account required</p><h2>Sign in to message sellers.</h2><p>DigitalDeviceHub keeps conversations tied to the device listing.</p><a class="btn" href="/?signin=1">Go to sign in</a>');if(s.user.id===listing.seller_id)return toast('This is your own listing.');modal(`<p class="eyebrow">Message seller</p><h2>${esc(listing.title)}</h2><form id="msgForm"><textarea class="control" name="body" rows="5" required maxlength="4000" placeholder="Ask about availability, condition, pickup or delivery."></textarea><button class="btn blue" style="width:100%;margin-top:12px" type="submit">Send message</button></form>`);document.querySelector('#msgForm').onsubmit=sendMessage}
  async function sendMessage(e){e.preventDefault();const s=session();const body=String(new FormData(e.currentTarget).get('body')||'').trim();try{let conv=await get(`/rest/v1/conversations?select=id&listing_id=eq.${encodeURIComponent(listing.id)}&buyer_id=eq.${encodeURIComponent(s.user.id)}&seller_id=eq.${encodeURIComponent(listing.seller_id)}&limit=1`,true);let cid=conv?.[0]?.id;if(!cid){const made=await write('/rest/v1/conversations','POST',{listing_id:listing.id,buyer_id:s.user.id,seller_id:listing.seller_id});cid=made?.[0]?.id}if(!cid)throw new Error('Could not start conversation.');await write('/rest/v1/messages','POST',{conversation_id:cid,sender_id:s.user.id,body});document.querySelector('#deviceDialog').close();toast('Message sent.')}catch(err){toast(err.message)}}
  document.querySelector('#messageSeller').onclick=message;document.querySelector('#saveDevice').onclick=favorite;document.querySelector('#shareDevice').onclick=async()=>{try{await navigator.share?.({title:listing?.title||document.title,url:location.href})||navigator.clipboard.writeText(location.href);toast('Listing link ready to share.')}catch{}};document.querySelector('#dialogClose').onclick=()=>document.querySelector('#deviceDialog').close();document.addEventListener('ddh:localization-ready',e=>document.querySelector('#localeChip').textContent=`${e.detail.country} · ${e.detail.currency}`);load();
})();