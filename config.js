// Public runtime configuration for DigitalDeviceHub.
// Supabase project URL and publishable key are intentionally blank until the free project exists.
// Publishable keys are designed for browser use; never place a service_role key here.
window.DDH_CONFIG = Object.freeze({
  supabaseUrl: '',
  supabasePublishableKey: ''
});
